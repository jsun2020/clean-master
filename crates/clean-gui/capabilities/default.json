{
  "$schema": "https://schema.tauri.app/reference/acl/capability",
  "identifier": "default",
  "description": "Main window: core APIs only (events + app commands). No plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
